/**
 * Application Camera controller
 * @author Martin Vach
 */

/**
 * Camera add controller
 */
myAppController.controller('CameraAddController', function($scope, $routeParams, dataFactory, dataService) {
});

/**
 * Camera manage controller
 */
myAppController.controller('CameraManageController', function($scope, $routeParams, dataFactory, dataService) {
});
