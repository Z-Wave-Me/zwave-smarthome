/**
 * @overview This is used to store the compiled and compressed views.
 * @author Martin Vach
 */

/**
 * Angular module initialization
 * @function myAppTemplates
 */
angular.module('myAppTemplates', []).run(['$templateCache', function($templateCache) {'use strict';}]);
